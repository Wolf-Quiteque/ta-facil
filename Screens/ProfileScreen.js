import { View, Text } from "react-native";

const ProfileScreen = () => {
  return (
    <View
      style={{
        height: "100%",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Conta</Text>
    </View>
  );
};

export default ProfileScreen;
